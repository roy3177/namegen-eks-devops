# Terraform
.terraform/
terraform.tfstate
terraform.tfstate.backup
terraform.tfvars
crash.log

# Node.js
app/node_modules/
app/.env

# Kubernetes secrets (real credentials — keep the .example template in git)
kubernetes/mongodb-secret.yaml

# OS / editor
.DS_Store
Thumbs.db
